export async function sendPasswordResetEmail(toEmail: string, resetLink: string) {
  // Phase 1 stub. Replace with SMTP provider later.
  console.log(`[EMAIL STUB] To: ${toEmail} Reset: ${resetLink}`);
}